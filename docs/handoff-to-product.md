# Handoff to product — ARCHIVED (pending exploratory promotion)

This doc described a `handoff-to-product` campaign that wired three missions
together: `scaffold-align`, `contract-first-build`, and `agents-layer`.

On 2026-06-22 all three missions moved to `docs/exploratory/missions/`
because they are not on the Stage 8 distillation list in the empirical
corpus (`prompts.md` L2960-2964) and have no `-progress.md` or
`-readiness.md` from a real run. See `docs/exploratory/README.md` for the
promotion rule.

The previous content of this doc (supported handoff convention,
methodology, OPS boundary, worked example) is preserved in git history at
the commit immediately preceding the move-to-exploratory commit. Restore
it together with the campaign YAML
(`scripts/campaigns/handoff-to-product.yaml`) when the first real run
validates the three missions and promotes them back to `skills/`.
