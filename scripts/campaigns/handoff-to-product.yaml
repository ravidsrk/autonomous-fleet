# handoff-to-product: ARCHIVED.
#
# This campaign was edited (effectively neutralized) on 2026-06-22 because
# three of its four building-block missions moved to docs/exploratory/:
#   - scaffold-align       → docs/exploratory/missions/scaffold-align
#   - contract-first-build → docs/exploratory/missions/contract-first-build
#   - agents-layer         → docs/exploratory/missions/agents-layer
#
# Those missions are not on the Stage 8 distillation list
# (prompts.md L2960-2964 in the source corpus) and have no real-run progress or
# readiness doc backing them.
#
# Re-promote the missions to skills/ (per docs/exploratory/README.md) on
# the first real run that validates each pattern, then restore the
# campaign DAG below from git history (commit immediately preceding the
# move-to-exploratory commit).
#
# Until then this campaign is intentionally empty so the autoplan / edge
# linter does not bind to undefined missions.
campaign: handoff-to-product
status: archived-pending-exploratory-promotion
