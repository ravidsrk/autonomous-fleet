# secure-ship: ARCHIVED.
#
# This campaign was archived on 2026-06-23 (Commit D) because the
# `deps` node referenced `dependency-update`, which moved to
# docs/exploratory/missions/dependency-update/ — it has no progress +
# readiness + external archive triple.
#
# Re-promote `dependency-update` per docs/exploratory/missions/README.md,
# then restore the campaign DAG from git history (commit immediately
# preceding the Commit-D demotion) — the audit-gated edge structure
# (`if: findings_open == 0`) is the load-bearing part to preserve.
#
# Until then this campaign is intentionally empty so the autoplan / edge
# linter does not bind to undefined missions.
# exploratory: dependency-update skipped until promoted
campaign: secure-ship
status: archived-pending-exploratory-promotion
