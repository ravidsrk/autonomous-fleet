Horizontal banner image, 2:1 aspect ratio (about 1200×600 feel).

PALETTE (strict):
  - Background: deep midnight #0B1220 (subtle cool gradient toward #0F172A at edges)
  - Primary accent: warm amber #F59E0B
  - Secondary accent: cool slate / steel blue #94A3B8
  - Highlight: white #FFFFFF (sparingly)
  - Ink for any text: warm off-white #F8FAFC

STYLE: Clean schematic / cockpit / control-room aesthetic. Engineering-serious, not cyberpunk. Linear.app + Vercel + GitHub-Actions dark-mode feel. Thin precise lines, generous spacing, subtle drop shadows. NOT photorealistic, NOT neon, NOT glowing-grid cyberpunk.

COMMON RULES:
  - Top-left corner: small monospace text exactly: "skills/scaffold-align"
  - Bottom-center: thin horizontal amber accent line above a one-line caption: "create new module aligned to existing patterns"
  - No fake domain names, no garbled words, no made-up text labels
  - All visible text must be ONLY the labels explicitly listed below — every label appears EXACTLY ONCE
  - No real brand logos (no GitHub/Anthropic/OpenAI/Cursor logos), only abstract iconic glyphs


COMPOSITION:
  LEFT half: 3 small slate-blue rectangles aligned in a perfect vertical column, each with identical internal structure (a small amber dot top-left, a thin slate-blue line below).
  RIGHT half: a single warm amber rectangle of the same dimensions, aligned to the same baseline as the column, also with the same internal structure (amber dot, slate line).
  Between them: a thin slate-blue dashed vertical alignment guide line spanning the full height.

  ABOVE the column, small monospace warm off-white text exactly: "EXISTING"
  ABOVE the amber rectangle, small monospace warm off-white text exactly: "NEW"

  Labels visible: "EXISTING", "NEW" — each exactly once. No other text besides top-left + caption.
